language: python

python:
  - "3.6"

before_install:
  - pip install virtualenv
  - virtualenv venv
  - pip install coala-bears
  - pip install coala-quickstart
  - echo | coala-quickstart

script:
  - coala  --non-interactive
